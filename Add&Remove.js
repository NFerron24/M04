let btn = document.getElementsByClassName(".big-five-list");
console.log(btn);

/* let btn = document.querySelector(".big-five-button");
const btnArray = new Array(5).fill(btn).map(i)=>i);
console.log(btnArray);
btn.addEventListener("click", (e) => {
  if (e.target !== e.currentTarget) {
    console.log(e.target.value);
  }
}); */
